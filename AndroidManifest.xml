<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="us.shandian.flashbak"
    android:versionCode="1"
    android:versionName="0.1a" >

    <uses-sdk 
        android:minSdkVersion="14" 
        android:targetSdkVersion="18" />

	<uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
		android:theme="@android:style/Theme.Holo.Light.DarkActionBar" >
        <activity
            android:label="@string/app_name"
            android:name=".ui.MainBackupListActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
		<activity
			android:label="@string/title_new_backup"
			android:name=".ui.NewBackupActivity"
			android:theme="@android:style/Theme.Holo.Light.DarkActionBar" />
		<activity
			android:label="@string/title_restore_backup"
			android:name=".ui.RestoreBackupActivity"
			android:theme="@android:style/Theme.Holo.Light.DarkActionBar" />
    </application>

</manifest>
